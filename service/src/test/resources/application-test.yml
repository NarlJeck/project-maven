db:
  username: test
  password: test

spring:
  datasource:
    url: jdbc:postgresql://${CONTAINER.HOST}:${CONTAINER.PORT}/${CONTAINER.DATABASE}
    username: test
    password: test

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop